import * as React from 'react';
import Layout from '../components/Layout';

export interface IGrindsInfoProps {
}

export default function GrindsInfo(props: IGrindsInfoProps) {
    return (
        <Layout hasNavbar hasFooter>
            GrindsInfo content goes here
        </Layout>
    );
}
